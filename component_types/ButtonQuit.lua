-- The ButtonAction component for the Menu Button.

ButtonQuit = {

    -- On Click function:
    -- Quit the application.
    OnClick = function (self)
        Application.Quit()
    end -- ON CLICK

} -- BUTTON MENU